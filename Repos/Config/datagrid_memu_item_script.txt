<script language="JavaScript">
<!--

function MenuItemClick(e) {
    
      
    if (e.item.name == "CustomizeColumns")
    {
         if(GridView.IsCustomizationWindowVisible())
            GridView.HideCustomizationWindow();
        else 
            GridView.ShowCustomizationWindow();
        e.processOnServer = false;

    }
    else if (e.item.name == "ActiveControl")
    {
        e.processOnServer = false;
        
    }
    else if (e.item.parent.name == "PivotViews")
    {
        e.processOnServer = false;
    }
    else if (e.item.parent.name == "Charts")
    {
        e.processOnServer = false;
    }
    else if (e.item.parent.name == "Algorithms")
    {
        e.processOnServer = false;
    }
    else if (e.item.parent.name == "Exports")
    {
         alert("当表中的数据记录很多时,导出过程需要较长时间.请耐心等待直到保存文件的窗口出现.");
    }
    else
    {
	e.processOnServer = e.item.GetItemCount() == 0; // Prevent generating a postback for parent menu items
    }
}
//-->
</script>